server {
	listen {{ nginx_backend_port }};
	server_name koscinski-dev.thejfk.ca;
	location / {
		proxy_set_header Host $http_host;
		proxy_pass http://koscinski-dev_backend;
	}
	include snippets/health-check.conf;
}
upstream koscinski-dev_backend {    
    server koscinski-dev.jfkhome;    
}
