- name: Build 
  hosts: dev
  serial: 1
  vars_files:
    - vars.yaml    
  tasks:
    - name: Run install tasks
      import_tasks: tasks/install.yaml

  handlers:
  - name: restart nginx
    service:
      name: nginx
      state: restarted
    become: true

